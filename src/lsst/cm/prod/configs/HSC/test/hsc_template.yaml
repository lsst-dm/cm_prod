includeConfigs:
- ${CTRL_BPS_PANDA_DIR}/config/bps_usdf.yaml
- ${CM_PROD_DIR}/src/lsst/cm/prod/configs/HSC/test/requestMemory.yaml

pipelineYaml: "${DRP_PIPE_DIR}/pipelines/HSC/DRP-RC2.yaml#step1"

LSST_VERSION: w_2022_36

payload:
  payloadName: "HSC/test"
  output: {w_coll_out}
  outputRun: "{w_coll_out}_{job_id}"
  butlerConfig: {butler_config}
  inCollection: {w_coll_in}
  dataQuery: {data_query}

executionButler:
  requestMemory: 64000
  queue: "SLAC_Rubin_Merge"

numberOfRetries: 3
